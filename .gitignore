.env
*.json
docker-compose.local.yml

# рабочие каталоги: файлы базы (bind-mount из deploy/docker-compose.yml)
# и собранные бинарники (make build). Те же исключены из контекста Docker
# правилом _* в .dockerignore.
_volume_db/
_bin/
